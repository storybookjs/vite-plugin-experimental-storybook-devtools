/**
 * Rsbuild Entry Point (placeholder)
 *
 * Rsbuild is not supported yet; Vite is the only supported bundler today —
 * see the phased migration in docs/plans/DEVFRAME_OPPORTUNITIES.md.
 */
export default function componentHighlighterRsbuild(): never {
  throw new Error(
    'vite-plugin-experimental-storybook-devtools/rsbuild is not supported yet; ' +
      'Vite is the only supported bundler today — see the phased migration in ' +
      'docs/plans/DEVFRAME_OPPORTUNITIES.md.',
  )
}
